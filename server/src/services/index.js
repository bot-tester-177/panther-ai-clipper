// service registry
// this file can be used to aggregate or lazily load services

const HypeEngine = require('./hypeEngine');

module.exports = {
    // export service constructors or instances
    HypeEngine,
};
